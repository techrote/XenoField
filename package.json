{
  "name": "abyssal-xenofield",
  "version": "4.4.1-alpha.0",
  "private": true,
  "type": "module",
  "description": "Native WebGPU visual-synthesis laboratory with cached coarse volumes, direct procedural-cloud world volumes, depth routing, independent temporal histories and kinetic compositing",
  "scripts": {
    "start": "node tools/serve.mjs",
    "test": "node --test tests/*.test.mjs && node tools/check.mjs",
    "build:standalone": "node tools/standalone.mjs",
    "test:gpu": "node tools/gpu-smoke.mjs",
    "test:ui": "python tools/ui-component-checks.py"
  },
  "engines": {
    "node": ">=20"
  }
}
